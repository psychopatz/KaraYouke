import { Typography } from "@mui/material";

const PlayerComponent = () => {
    return (  
        <>
        <Typography>Player Component</Typography>
        </>
    );
}
 
export default PlayerComponent;